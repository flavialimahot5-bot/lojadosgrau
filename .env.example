# Required: configure only in Vercel > Settings > Environment Variables > Production.
# Never put real values in Git or use NEXT_PUBLIC_, VITE_ or PUBLIC_ prefixes.
IRONPAY_API_TOKEN=
IRONPAY_PRODUCT_HASH=
IRONPAY_OFFER_HASH=
